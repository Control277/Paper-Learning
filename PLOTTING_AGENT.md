# Python 实验数据分析与绘图 Agent 通用模板（讨论稿）

本模板作为其他 Agent 的嵌入式绘图规则，用于 Python 实验数据分析与绘图。沿用调用方的任务上下文，使用 Matplotlib 绘图，按需使用 NumPy、pandas 处理数据。

## 绘图规范

### 脚本结构

- 绘图脚本统一命名为 `plot_<实验名>.py`，即 `plot` + 下划线 + 实验名，例如 `plot_throughput.py`。
- 一个 Python 脚本基本只对应一个实验数据目录，按当前实验直接编写，尽可能保持结构简单。
- 不设置命令行输入参数，不建立可配置全局参数区或通用配置系统；数据路径、绘图数值和样式在相关代码位置直接明确写出。 显示名称字典是允许保留的手动编辑项，不扩展为通用配置系统。
- 不为跨实验复用进行通用化封装；仅在确有重复逻辑且能简化代码时使用少量辅助函数。

### 数据与表达

- 单位、归一化基准、误差含义和坐标轴变换应明确；缺失数据不得当作零值。
- 二进制数量使用 IEC 前缀，十进制数量使用 SI 前缀；必要时保留精确值。
- 同一比较对象在不同子图中保持一致的颜色、线型和名称。
- 数据集、数据系列及 legend 中的显示名称统一由脚本内显式定义的字典维护，例如 `display_names = {"raw_a": "Method A", "raw_b": "Method B"}`；键为原始标识，值为可手动修改的展示名称。
- 初次编写脚本时可根据数据文件或日志中的原始名称填入字典，默认展示名称可以与原始名称相同，但须将映射逐项写入脚本，不在每次运行时自动生成名称字典。绘图时通过 `display_names[raw_name]` 获取名称，不直接用原始名称作为图例或其他展示标签，也不以原始名静默回退；新增系列时补充字典。

### 尺寸与布局

- 显式设置画布尺寸。论文图默认单栏宽度为 3.33 英寸，双栏宽度为 7.0 英寸，高度按内容设置，即 `figsize=(3.33, height)` 或 `figsize=(7.0, height)`；目标出版模板明确规定其他栏宽时，以该模板为准。
- 插图插入论文时必须零缩放，保持生成文件的原生物理尺寸；使用 `\includegraphics{...}`，不设置 `width`、`height`、`scale`，不使用其他缩放包装。单栏或双栏尺寸在 Python 绘图时确定，双栏图使用 `figure*`。
- 需要严格物理尺寸时，使用显式布局或 `subplots_adjust(...)`，不使用 `tight_layout()` 或 `bbox_inches="tight"`，保存时设置 `pad_inches=0`。
- 每个图及子图的坐标轴范围都必须显式设置，所有 `lim` 的上下界直接填写固定的绝对数值，例如 `ax.set_xlim(0, 100)`、`ax.set_ylim(0, 50)`。不得使用 `max`、`min`、`np.max`、`np.min` 或其他数据极值、计算表达式来生成边界，也不依赖自动缩放。
- 坐标轴刻度位置必须显式设置，例如显示 0–100 的范围且仅展示 0、50、100 三个刻度时，使用 `ax.set_xticks([0, 50, 100])`；y 轴同样使用 `ax.set_yticks(...)`。不依赖自动刻度分割；启用次刻度时也须显式指定其位置。
- 先设置刻度，再设置最终坐标轴范围，避免设置刻度时自动扩展范围；刻度标签间距仍须显式设置 `pad`。
- 每个图及子图都必须显式设置上框线和右框线的可见性，分别写出 `ax.spines["top"].set_visible(True)` 和 `ax.spines["right"].set_visible(True)`；布尔值按所需效果直接填写 `True` 或 `False`，便于独立调整，不依赖默认样式。
- 图例优先放在绘图区上方，不遮挡数据；子图编号、间距与对齐方式保持一致。
- 所有图标题、子图标题和坐标轴标题均使用 `fig.text(...)` 或 `ax.text(...)` 绘制，不使用 `title`、`set_title`、`suptitle`、`xlabel`、`ylabel`、`set_xlabel` 或 `set_ylabel` 等标题接口。显式指定文字坐标、坐标系及对齐方式，便于调整位置。
- 所有设置坐标轴刻度标签位置的代码都必须显式指定 `pad`，不得依赖默认间距。使用 `ax.tick_params(axis=..., which=..., pad=...)` 设置刻度标签与坐标轴的距离；主刻度、次刻度及附加坐标轴有标签时均应覆盖。`set_xticklabels(...)` 等不支持 `pad` 的接口，应在相邻代码中配套调用 `tick_params(...)`。

### 字体与样式

- 最终呈现的所有图中文字至少为 6 pt，无需放大即可辨读。
- 显式设置字号：刻度标签使用 6 pt，图例和轴标题使用 7 pt，面板或图标题使用 7 pt 加粗；组合图 `(a)`–`(d)` 等子图编号使用 8 pt 加粗。
- Matplotlib 字号按原生物理点数设置，插入论文时保持不变。内容过密时调整绘图布局，不通过缩放插图解决。
- 字体、线宽和配色保持统一；颜色应有足够对比度，必要时结合线型、标记或填充区分。
- 红色优先用于强调、警告或界限，避免与普通数据系列混淆。
- Matplotlib 导出 PDF/PS 时使用 `pdf.fonttype = 42` 和 `ps.fonttype = 42`。

### 输出与维护

- 输出目录由任务指定；文件名清晰、唯一，优先复用并修改已有绘图源码。
- 论文和报告图优先使用 PDF 等矢量格式，其他场景按用途选择 SVG 或 PNG。
- 验证替代产物后再清理旧图，避免保留容易混淆的重复版本。

## 结果检查

- 数值、标签、单位和图例与输入数据一致。
- 无文字重叠、内容裁切或无法辨认的视觉元素。
- 输出尺寸和格式符合任务要求，Python 脚本可从指定实验数据重新生成分析结果与图表。
- 在实际使用尺寸下检查生成图片，不能仅凭脚本运行成功判断绘图完成。

## 按任务配置

论文图默认采用上述栏宽和字号；画布高度、字体、配色、输出目录及领域术语按任务配置。目标出版模板或用户另有明确要求时，相应调整。这些要求直接落实到对应实验脚本中，不额外建立通用参数配置层。
